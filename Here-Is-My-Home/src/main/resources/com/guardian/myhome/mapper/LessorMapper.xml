<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 
 <mapper namespace="com.guardian.myhome.mapper.LessorMapper">
 
 	<!-- 회원가입 -->
 	<insert id="lessorJoin" parameterType="com.guardian.myhome.vo.LessorVO">
	insert into lessor_tb (lessor_id, lessor_pw, lessor_nickname, phone,name,birthDate,jgs_name,jgs_num, user_roll, lessor_addr1, lessor_addr2, lessor_addr3)          
	values(#{lessorId}, #{lessorPw}, #{lessorNickName}, #{phone},#{name},#{birthDate},#{jgsName},#{jgsNum},#{userRoll}, #{lessorAddr1}, #{lessorAddr2}, #{lessorAddr3})
 	</insert> 
 	
 	<!-- 아이디 중복검사 -->
 	<select id="idCheck" resultType="int">
 		select count(*) from lessor_tb where lessor_id = #{lessorId}
 	</select>
 	
 	<!-- 닉네임 중복 검사 -->
<!--  	<select id="nicknameCheck" resultType="int">
 		select count(*) from lessor_tb where lessor_nickname = #{lessorNickName}
 	</select> -->
 	
 	<!-- 로그인 -->
 	<select id="lessorLogin"  parameterType="com.guardian.myhome.vo.LessorVO">
 			select lessor_id AS lessorId, lessor_nickname AS lessorNicknamefrom lessor_tb where lessor_id = #{lessorId} and lessor_pw = #{lessorPw}
 	</select>
 	
 </mapper>